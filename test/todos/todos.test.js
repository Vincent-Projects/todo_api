/* const expect = require('chai').expect;
const TodosDAL = require('../../src/todos').TodosDAL;
const TodoService = require('../../src/todos').TodoService;

describe('Todos Model', function () {
    describe('Todos DAL', function () {
        it("When no data is provided, should return false", async function (done) {
            done();
        });

        it("When task is provided, should return a task object with 201 status code", function (done) {
            done();
        });
    });

    describe('Todos Service', function () {
        it("When no data is provided, should return an error object with 401 status code.", async function (done) {
            done();
        });

        it("When task is provided, should return a task object with 201 status code", function (done) {
            done();
        });
    });
}); */